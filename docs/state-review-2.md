# Triggered states, second look — findings

2026-09-01, after the closeout fixes landed (separator rule in the
component, drawer blur 10, whisper-capped glass overlay). Refusal and
Tool partly fails re-triggered in full page × both themes × both
expressions on the finished surface; the failure→recovery chain
re-recorded to `docs/media/state-recovery.webm`. Filed, not fixed —
same rule as `docs/state-review.md`, whose work list still stands.

## New findings

1. **The refusal lead diverges by theme.** "Declined." renders
   foreground-white in dark and accent-ink violet in light. One of
   these is the design; the other inherited it. Decide which — the
   dark treatment (plain foreground weight) reads more like the
   house's no-colour-escalation position.
2. **The partial-failure receipt on light Glass** sits on the newly
   whisper-capped chip and reads clean — the caution ink is the only
   colour on the object, which is the point. No action; recorded as
   the reference for how tone-on-object should look.
3. **Both states pass the separator rule** — the divider above each
   triggered exchange has the full prior answer visible above it in
   every cell.

## Standing (from state-review.md, unresolved)

Version badge arithmetic (v4 where a reader expects v2); superseded
turns keep full affordance weight; refusal glyph optical weight at
distance; Restore arrives unintroduced; README embed format for the
recording.
