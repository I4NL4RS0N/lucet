---
"lucet-core": patch
"lucet-react": patch
---

Budget Meter, component audit 03. Prices in the picker align in one column (every row reserves the check slot); the trigger's estimate keeps a stable slot so a draft crossing the cent no longer moves the chip; the month bar's track is visible against the panel and the record fill is ink; the hold's two buttons carry their price in the button's own colour and size; the locked trigger leaves the Tab order, cancels its toggle and exposes `aria-disabled`; the hold's note says what each way does. Core: a runtime test records the rule that a running turn is never stopped for cost.
